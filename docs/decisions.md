# 결정 로그

brief.md 이후 확정된 사항. 재논의 금지 원칙은 동일.

| # | 결정 | 출처 |
|---|---|---|
| 1 | 저장소는 **SQLite** (공시·일정·본주·인수권 시세를 한 DB에) | GPT 인수인계 대화 Q9 |
| 2 | 증권사는 **미래에셋** — 실제 주문 + 화면 눈검증용으로만 사용 | GPT 대화 Q10 |
| 3 | 데이터 소스: **DART**(공시·정정·일정·발행조건), **KRX Open API**(본주 + 신주인수권증서 일별 시세) | GPT 대화 Q11 |
| 4 | KRX `sr_bydd_trd`가 2010-02-12부터 제공 → **H1(인수권 괴리)도 과거 백테스트 가능성** 생김. brief §4의 "과거 데이터 확보 어려움" 전제는 실측 후 갱신 | GPT 대화 Q11 |
| 5 | 실행 위치: ~~Windows 로컬 + 작업 스케줄러~~ → **별도 Netlify 웹 + GitHub Actions 자동 갱신** (KR헤게모니와 같은 구조) | 사용자 지시 (2026-09-25) |
| 6 | SQLite 바이너리는 커밋하지 않고 **텍스트 덤프 `data/yujeung.sql`을 커밋** (git 히스토리 비대화 방지). 실행 때마다 덤프 → DB 복원 → 갱신 → 덤프 | 구현 결정 |

## 2026-09-25 추가 결정 (사용자 지시)
| # | 결정 |
|---|---|
| 7 | 텔레그램 발송 제거. 알림 문구 생성·기록만 유지하고, 사이트 맨 위 **'오늘 볼 것'**(🔴 인수권 마감 D-2 · 🟡 권리락·발행가확정·청약·신주상장 D-1 · 🟢 최근 3일 신규 주주배정)이 알림 역할 |
| 8 | 수집 대상은 코스피·코스닥(Y·K)만. 보존 기준: 신주 상장일+30일 ≥ 오늘 또는 상장일 미확인. 가상 성과 기록이 있는 케이스는 지우지 않고 '종료' |
| 9 | 제외: 증자방식·금액이 빈 공시, 청약일=납입일인 비주주배정(내부 증자). 제외 공시는 `excluded_disclosures` 에 남겨 재실행 때 다시 받지 않음 |
| 10 | 정정 표시 없이 다시 낸 원공시(같은 회사·증자방식·신주수, 60일 이내)는 같은 케이스로 합침 (셀리드 사례) |
| 11 | 자동 판정: 관문1 = 채무상환<50% · 희석<50%(≥100% 즉시 탈락) · 최대주주 전량 이상 청약 · 직전 분기 영업흑자 · 52주 위치≤50% · 총액(잔액)인수. 하나라도 탈락이면 관문1 탈락, 미확인은 통과를 막지 않되 표시. 업계 순위·대체 불가는 'GPT 확인 필요'. 관문2 = 괴리율 ±20% |
| 12 | 판정: 괴리 ≥ +20% → 🔵 / 관문1 통과 + 괴리 ≤ −20% → 🟢 / 관문1 통과 → 🟡 / 그 외 → ⚪(관찰 샘플) |
| 13 | 리츠는 차환 유증이 일상 — 채무상환 기준 완화 여부는 **표시만** (미적용) |
| 14 | 가상 성과: 인수권 마지막 거래일 종가가 들어온 순간 판정·근거를 스냅샷으로 저장(불변). 🟢⚪ 인수권 종가+발행가 → 상장일 시가/+5/+20 · 🟡 상장일 종가 → +5/+10/+20 · 🔵 (가격−발행가)÷인수권 매도가−1 → **09-26 수정: 모든 판정 분모 = 투입 원금(인수권+발행가 / 🟡 본주 매수가), 🔵은 "팔지 않았다면" 수익률(낮을수록 적중)**. 지수(코스피/코스닥) 대비 초과수익 %p. 확정발행가가 판정 당시보다 낮으면 원가에 반영(표시) |
| 15 | 성적표는 표본 수를 정보로만 표시하고 매매 제한 문구는 넣지 않음 |

## 2026-09-26 추가 결정 (사용자 지시 [8][9])
| # | 결정 |
|---|---|
| 16 | 카드 최상단(판정 배지 바로 아래) **30초 결론**: 🟢 매수 검토 / 🟡 상장일 대기 / 🔵 인수권 매도 / ⚪ 패스 + 이유 한 줄(관문1 탈락 사유 또는 괴리율) + 다시 볼 조건 한 줄 |
| 17 | 발행가 추정: 권리락 전 I1 = P(1−d)/(1+r·d), Px = P/(1+r·d) / 권리락 후 I2 = P(1−d)(권리락 **후** 주가), 최종 = min(I1, I2) / 확정발행가 공시 후엔 확정값. d = 증권신고서 할인율. 구현상 권리락 후 I1 은 공시된 1차 발행가 우선 |
| 18 | 본전선: ① 권리락 전 본주+청약 = Px / ③ 인수권 거래 중 인수권+청약 = 인수권 + 발행가 / ⑤ 상장 대기 본주 = 현재가 (②·④ 도 할 수 있는 건 본주 매수라 ⑤ 기준). 어림 손익표 = 본전선 대비 +15/0/−15/−30%, 고정 문구로 "Phase 1 백테스트 후 25/50/75 퍼센타일로 교체 예정". 매물 소화일수 = 신주 수 ÷ 최근 20거래일 평균 거래량, 60일 이상 빨간색 |
| 19 | 개별기업 프롬프트 출력 순서: 30초 결론 → 어림 손익표(본전선) → 다시 볼 조건 → 상세 분석. 2차 발행가는 권리락 후 주가 경고 포함 |
| 20 | 정정공시가 뜨면 옛 일정은 통째로 폐기, **가장 최근 유상증자결정 원문 하나**가 기준(그 뒤 증권신고서 값만 덮음). 원문 속 '정정전/정정후' 표는 본문 파싱에서 제외하고 빈칸만 정정후 값으로 |

## 본주 종가 출처 차이 (2026-09-25 실측) — 앞서 적은 "브리프 오기" 판단을 철회
- SK디앤디 9/23 종가: **KRX 정규장 3,335원** (stk_bydd_trd·sr_bydd_trd 모두) vs **네이버 일봉 3,395원** (= 브리프 값)
- 브리프가 틀린 게 아니라 출처가 다름. 네이버는 NXT(넥스트레이드) 포함 통합시세라 종가가 다를 수 있다는 가설 — **검증 필요**
- 결정: 인수권은 KRX 에서만 거래되므로 **괴리율·판정은 KRX 종가 우선**(9/23 이론가 1,075원·괴리율 −40.8%). 네이버는 KRX 값이 없는 날(52주 위치, 상장 후 측정)만 보충
- 결론(A 통과 / B 탈락 / 관찰용 케이스)은 영향 없음

## 미결 질문 (스무고개 이어가기)
- Q12 OpenDART 인증키 → KR-hegemony가 `DART_API_KEY`로 이미 쓰는 중이므로 **보유**로 간주. 이 레포 Secrets에만 등록하면 됨.
- 다음: KRX Open API 인증키·서비스 이용신청 여부

## 2026-09-26 [10] 과거 유증 백테스트 (사용자 지시)
| # | 결정 |
|---|---|
| 21 | 대상 2020-01~2026-06 주주배정 상장사, 연도별 실행(`backtest --year`). 인수권 시세는 케이스별 거래기간 날짜만 KRX 조회, 본주는 KRX 우선(상장폐지 포함) |
| 22 | 미래 정보 금지: 판정일 = 인수권 마지막 날. 영업이익은 판정일까지 법정 제출기한이 지난 보고서, 52주는 공시일 기준. 판정 로직·가상 진입은 verdict.py·paper.py 그대로 |
| 23 | 파싱 실패는 제외하되 연도별 파싱 성공률 필수 리포트. 결과는 data/backtest.json + 사이트 '기출문제 성적표', 한계는 docs/backtest.md |
| 24 | 카드 손익표 기본 시나리오 → 판정별 실제 분포(인수권 매수+청약 → 상장일 시가의 75/50/25 퍼센타일·최저), 표본 5건 미만이면 기본값 유지 |
| 25 | 발행가 구분(예정/1차/확정): 라벨에 '확정발행가'·'1차'·'예정발행가'가 있으면 그대로, 없으면 그 값을 낸 공시일로 — 확정 산정일 이후 = 확정, 1차 산정일(기준일 전 3거래일) 이후 = 1차, 그 외 예정. **I1 은 1차일 때만 공시값**, 예정이면 권리락 직전 종가(권리락 전이면 현재가)로 계산 (결정 17 의 '공시값 우선'을 좁힘) |

## 2026-09-26 [11] 검수 결과 수정 (사용자 지시)
| # | 결정 |
|---|---|
| 26 | 정정공시: 본문 표('1. 신주의 종류와 수') 앞 구간은 머리글 문구와 상관없이 정정·표지로 보고 일정 파싱에서 제외. 그 구간 행은 뒤쪽 절반(정정후)만 빈칸 보충용 (이렘 9/23 재발) |
| 27 | 청약 시작일 이후에는 발행가 재추정 금지 → 공시 발행가를 확정가로 사용. 확정 산정일이 없으면 청약 전 3거래일 이후 공시를 확정으로 본다. 가상 성과 원가도 확정 공시 또는 청약 시작 후엔 최신(확정) 발행가 |
| 28 | 🟢(🔵 포함) 판정인데 인수권 거래가 끝났으면 30초 결론 = "진입 구간 종료 — 성과 추적 중" |
| 29 | 권리락 후 인수권 상장기간이 미정이면 단계 = "인수권 일정 미정" |
| 30 | 정정 구간 판별은 '본문 시작 후보(맨 앞 + 신주의 종류와 수 표마다)' 중 항목 행을 찾은 칸(값 또는 추후결정)+정정후로 채울 수 있는 칸이 가장 많은 것, 동점이면 뒤쪽 |
| 31 | 정정공시는 자기보다 '먼저' 시작한 같은 회사 케이스(240일 이내)에만 붙인다. DART·KRX·네이버 호출은 연결 오류·시간 초과 시 5초·20초 뒤 재시도 |
| 32 | 행에 '추후결정/미정'이 있으면 그 항목은 미정 — 같은 행의 날짜는 정정전 값이라 쓰지 않는다. 최신 정정이 일정을 전부 미정으로 바꾸면 그 정정이 기준(이전 날짜로 되돌아가지 않음), 단계 = '인수권 일정 미정' (실측 경남제약 9/18: 금감원 정정요구로 기준일·납입일·상장일 추후결정) |

## 2026-09-26 사이트 모바일 가독성 (사용자 지시)
| # | 결정 |
|---|---|
| 33 | 폭 600px 이하: 인수권 표 → 카드(괴리율 크게), 가로 타임라인 → 세로 일정표(D-n·진행 중·지남), 표 첫 칸 고정. 케이스 카드는 회사명 → 판정 → 30초 결론 순. 데스크톱 화면은 그대로 |
| 34 | PR 시험 실행(push)은 동시 실행 줄을 따로 쓴다 — 데이터 커밋이 없으므로 대기 중인 backtest·daily 를 밀어내지 않게 |

## 2026-09-26 백테스트 속도 (사용자 지시)
| # | 결정 |
|---|---|
| 35 | 백테스트 꼬리 기간(+120일)은 '그 회사 케이스가 이미 있을 때만' 상세 조회. 지시("정정만")를 그대로 쓰면 이듬해 새 유증의 정정이 그해 케이스에 붙어 결과가 바뀔 수 있어(240일 창) 이 규칙으로 — 케이스·공시 묶음은 생략 전과 동일, 2020 실측 비교로 확인 |

## 2026-09-26 2020 파싱 실패 보완 (사용자 지시, 원문 26건 실측)
| # | 결정 |
|---|---|
| 36 | 🔵 수익률 분모 = 투입 원금(인수권+발행가) — −100% 밑 불가, 테스트로 고정 |
| 37 | 인수권 기간: 결정 공시 본문 "상장예정기간 : 날짜 ~ 날짜"를 먼저 찾고, 기준일 이전 날짜 후보(이사회결의일·전자증권 시행일)는 건너뛰고 계속 탐색. 비어 있으면 증권신고서 본문 일정표("날짜 ~ 날짜 신주인수권증서 상장 거래기간")에서 가져온다. 마지막 정정보다 먼저 나온 증권신고서라도 지금 일정(기준일 뒤·청약 전)에 맞으면 인수권 기간만 쓴다 (파서 v11) |
| 38 | '신주인수권증서의 상장여부 아니오' = 인수권 비상장 주주배정 → 백테스트 파싱 대상에서 제외(no_rights_listing), 전체 대비 비율도 함께 표시 |
| 39 | piicDecsn 캐시: 같은 회사 두 번째 공시가 첫 조회 구간 밖이면 다시 조회 — 제3자배정이 첫 유증(주주배정) 필드를 받아 주주배정 케이스로 잘못 잡히던 문제(2020 심텍·초록뱀·다이나믹솔루션) |

## 2026-09-26 [13] 디자인: 레이스 바 + 괴리율 온도계 (사용자 지시, 화면만)
| # | 결정 |
|---|---|
| 40 | 주주배정 카드 결론 박스 아래 **레이스 바** 6칸(공시·권리락·인수권·청약·상장·+20거래일): 지난 칸 채움 / 현재 깜빡이는 점(인수권은 거래일 진행률만큼) / 앞 칸 회색 / 날짜 없으면 점선+"미정". 오른쪽 D-n 다음 이벤트, 칸 누르면 날짜. 색 = 판정 색. 공시일보다 이른 날짜는 오파싱으로 보고 미정 처리 |
| 41 | **괴리율 온도계**(반원 −60~+60%, 범위 밖은 끝 고정): 경계 = site.json gap_threshold(관문 2 판정 기준 ±20%) — 파랑 저평가 / 회색 적정 / 빨강 고평가 + 글자 라벨. 큰 숫자 + 인수권 거래일별 추이선. 거래 전 회색 + "인수권 거래 전 (MM/DD~)", 끝나면 마지막 날 값 "최종". 기존 괴리율 스파크라인 자리 대체 |

## 2026-09-26 [16] 🟢 판정 엄격화 A안 (사용자 지시)
| # | 결정 |
|---|---|
| 42 | 관문1 자동 6개(채무상환·희석·최대주주 청약·영업흑자·52주·인수방식) 중 미확인이 하나라도 있으면 🟢 → **🟢? 확인 필요**(green_q), 🟡 → **🟡? 확인 필요**(yellow_q). 업계 1~3위·대체 불가(수동 2개)는 제외. LOGIC_VERSION 2 — 이미 저장된 판정 스냅샷(v1)은 불변 |
| 43 | 확인 필요의 진입·측정은 원래 판정(🟢 인수권+청약 / 🟡 상장일 종가)과 같고, 가상 성과·백테스트는 따로 집계("미확인 포함 🟢가 얼마나 틀렸나" 비교용). 화면: 미확인 항목명 빨간 글씨 + 그 항목만 묻는 GPT 프롬프트 복사, 정렬 🟢 · 🟡 · 🟢? · 🟡? · 🔵 · ⚪ |
